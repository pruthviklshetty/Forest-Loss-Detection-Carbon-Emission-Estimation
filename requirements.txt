# ---------------------------------------------------------------------------
# Forest Loss Detection & Dynamic Carbon Emission Estimation
# Pinned environment. Target interpreter: CPython 3.11 (3.11.x).
#
# CPU install (Colab CPU, or a machine without CUDA) works with the pins below
# as-is. For local NVIDIA GPU training, install torch + torchvision FIRST from
# the CUDA wheel index, then run `pip install -r requirements.txt`:
#
#   pip install torch==2.5.1 torchvision==0.20.1 \
#       --index-url https://download.pytorch.org/whl/cu121
#
# On Google Colab, torch/torchvision are already present and usually satisfy
# these pins; if Colab ships a newer torch, keep Colab's and install the rest.
# ---------------------------------------------------------------------------

# --- Deep learning ---
torch==2.5.1
torchvision==0.20.1
segmentation-models-pytorch==0.3.4
timm==0.9.7

# --- Numerics / classical ML ---
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.5.2
pandas==2.2.3

# --- Geospatial I/O ---
rasterio==1.3.11
shapely==2.0.6
pyproj==3.7.0

# --- Image processing ---
opencv-python-headless==4.10.0.84
Pillow==10.4.0

# --- Earth Engine data pull ---
earthengine-api==0.1.408
requests==2.32.3

# --- Config / plotting / utils ---
PyYAML==6.0.2
matplotlib==3.9.2
tqdm==4.66.5
