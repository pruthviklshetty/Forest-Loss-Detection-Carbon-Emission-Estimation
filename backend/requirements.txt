# Backend-only additions. Install the project requirements first:
#   pip install -r ../requirements.txt
# then:
#   pip install -r requirements.txt
#
# torch/rasterio/earthengine-api/Pillow/scipy all come from ../requirements.txt.

fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
