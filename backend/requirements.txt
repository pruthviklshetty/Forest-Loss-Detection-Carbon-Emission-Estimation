# The backend needs no packages beyond the project's root requirements.txt,
# which now includes fastapi / uvicorn / pydantic. Install once from the repo
# root:
#     pip install -r requirements.txt
#
# This file is kept as an explicit record of the serving-only dependencies.
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
